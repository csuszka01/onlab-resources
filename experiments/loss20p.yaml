apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: openfaas-loss-20
  namespace: chaos-mesh
spec:
  action: loss
  mode: all
  selector:
    labelSelectors:
      'faas_function': 'figlet'
    namespaces:
      - openfaas-fn
  loss:
    loss: '20'
    correlation: '0'
  duration: '2m'
